import React from 'react';
import type { GameState } from '../../../../shared';


export const GameScreen = ({ gameState }: { gameState: GameState }) => {
    return (
        <div>
            <h1>Game Screen</h1>
            
        </div>
    );
};