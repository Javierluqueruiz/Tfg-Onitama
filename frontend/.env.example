# Opcionales -- sin definir, el frontend usa rutas relativas al propio origen
# (proxeadas a localhost:3000 por el propio servidor de Vite). Solo hace
# falta ponerlas si el backend vive en otro sitio distinto del proxy.
# VITE_SOCKET_URL=http://localhost:3000
# VITE_API_URL=http://localhost:3000
VITE_TURNSTILE_SITE_KEY =